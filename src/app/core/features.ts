// Feature flags. Catalogue tabs are built but switched off until we re-introduce them.
// Set catalogueTabs to true (and CATALOGUE_TABS_ENABLED=true on Rails) to turn the feature back on.
export const features = {
  catalogueTabs: false
};
